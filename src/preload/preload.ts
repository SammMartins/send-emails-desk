// Preload script for Electron
// This file runs in the renderer process before the web page is loaded
// It has access to both DOM APIs and Node.js APIs

// Currently minimal as we're using the backend API server for all data access
console.log('Preload script loaded');
